import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.gh.decentraland',
  appName: 'Decentraland Explorer',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
